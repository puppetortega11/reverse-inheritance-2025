# Production Environment Variables for Railway Deployment
# Copy these to your Railway project variables

# Required: OpenAI API Key for AI Strategy Generation
OPENAI_API_KEY=sk-your-openai-api-key-here

# Required: Solana RPC URL (use a reliable RPC provider)
SOLANA_RPC_URL=https://api.devnet.solana.com

# Optional: Custom port (Railway will set this automatically)
PORT=8000

# Optional: Node environment
NODE_ENV=production

# Security Note: Never commit this file with real API keys!
# Add .env to your .gitignore file
